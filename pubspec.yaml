name: json_store
description: A Flutter storage solution somewhat similar to a browser's localStorage (but super powered)
version: 2.0.1
repository: https://github.com/fstof/flutter_json_store
issue_tracker: https://github.com/fstof/flutter_json_store/issues
homepage: https://github.com/fstof/flutter_json_store

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  encrypt: ^5.0.1
  flutter_secure_storage: ^8.0.0
  path_provider: ^2.0.12
  sqflite: ^2.2.4+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
